function sum(n1, n2) {
  return n1 + n2;
}

function name(name) {
  console.log(`hello ${name}`);
}

const num = 52

// modulo os
const os = require(`os`)

// modulo export
module.exports = {sum, name, num};
